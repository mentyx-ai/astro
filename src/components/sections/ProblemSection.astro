---
// No imports needed for this component
---

<section class="section problem-section">
  <div class="container">
    
    <h2 class="section-heading" data-aos="fade-up">
      <span class="text-red">Manual underwriting, disconnected spreadsheets,</span><br/>
      and endless PDFs slow every deal
    </h2>
    
    <p class="section-lead" data-aos="fade-up" data-aos-delay="100">
      <strong class="rose-highlight">Every missed day costs you a deal.</strong> Every manual check hides a risk.
    </p>
    
    <p class="section-description" data-aos="fade-up" data-aos-delay="200">
      Mentyx replaces fragmented workflows with one intelligent platform — from borrower application to approval.
    </p>

    <!-- Stats Grid -->
    <div class="stats-grid">
      
      <div class="stat-card stat-blue" data-aos="fade-up" data-aos-delay="0">
        <div class="stat-icon-small">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="12 6 12 12 16 14"/>
          </svg>
        </div>
        <div class="stat-number">60%</div>
        <div class="stat-label">Faster decisions</div>
      </div>
      
      <div class="stat-card stat-cyan" data-aos="fade-up" data-aos-delay="100">
        <div class="stat-icon-small">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
            <polyline points="14 2 14 8 20 8"/>
            <line x1="16" y1="13" x2="8" y2="13"/>
            <line x1="16" y1="17" x2="8" y2="17"/>
          </svg>
        </div>
        <div class="stat-number">90%</div>
        <div class="stat-label">Fewer document errors</div>
      </div>
      
      <div class="stat-card stat-purple" data-aos="fade-up" data-aos-delay="200">
        <div class="stat-icon-small">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
          </svg>
        </div>
        <div class="stat-number">Real-time</div>
        <div class="stat-label">Affiliate reporting</div>
      </div>

      <div class="stat-card stat-green" data-aos="fade-up" data-aos-delay="300">
        <div class="stat-icon-small">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 6L9 17l-5-5"/>
          </svg>
        </div>
        <div class="stat-number">40%</div>
        <div class="stat-label">Higher productivity</div>
      </div>

    </div>
  </div>
</section>

<style>
  /* PROBLEM SECTION SPECIFIC STYLES */
  .problem-section {
    background: linear-gradient(180deg, 
      rgba(15, 23, 42, 0.95) 0%, 
      rgba(30, 41, 59, 0.4) 50%, 
      rgba(15, 23, 42, 0.95) 100%
    );
    position: relative;
  }
  
  .problem-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 50% 50%, rgba(239, 68, 68, 0.08) 0%, transparent 60%);
    pointer-events: none;
  }

  .section-heading {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 900;
    margin-bottom: 24px;
    text-align: center;
    line-height: 1.2;
  }

  .text-red {
    background: linear-gradient(135deg, #ff6b6b 0%, #ee5a6f 50%, #ff8787 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    text-shadow: 0 0 30px rgba(255, 107, 107, 0.3);
  }

  .section-lead {
    font-size: 1.25rem;
    text-align: center;
    margin-bottom: 16px;
    color: #f1f5f9;
  }
  
  .rose-highlight {
    background: linear-gradient(135deg, #fb7185 0%, #f43f5e 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    font-weight: 700;
  }

  .section-description {
    font-size: 1.125rem;
    text-align: center;
    margin-bottom: 48px;
    color: var(--slate-400);
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  /* Stats Grid - Compact Version */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
    margin-top: 48px;
  }

  .stat-card {
    background: linear-gradient(145deg, rgba(30, 41, 59, 0.8) 0%, rgba(15, 23, 42, 0.9) 100%);
    border: 1px solid var(--slate-700);
    border-radius: 18px;
    padding: 24px 16px;
    text-align: center;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 180px;
    position: relative;
    overflow: hidden;
  }
  
  .stat-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 50% 0%, currentColor 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.4s ease;
  }
  
  .stat-card:hover::before {
    opacity: 0.1;
  }

  .stat-card:hover {
    transform: translateY(-6px) scale(1.02);
    box-shadow: 0 12px 40px rgba(59, 130, 246, 0.25);
  }

  .stat-blue {
    border-color: rgba(59, 130, 246, 0.4);
    color: #60a5fa;
  }

  .stat-blue:hover {
    border-color: rgba(59, 130, 246, 0.8);
    box-shadow: 0 12px 48px rgba(59, 130, 246, 0.35), 0 0 0 1px rgba(59, 130, 246, 0.2) inset;
    background: linear-gradient(145deg, rgba(30, 41, 59, 0.9) 0%, rgba(37, 99, 235, 0.15) 100%);
  }

  .stat-cyan {
    border-color: rgba(6, 182, 212, 0.4);
    color: #22d3ee;
  }

  .stat-cyan:hover {
    border-color: rgba(6, 182, 212, 0.8);
    box-shadow: 0 12px 48px rgba(6, 182, 212, 0.35), 0 0 0 1px rgba(6, 182, 212, 0.2) inset;
    background: linear-gradient(145deg, rgba(30, 41, 59, 0.9) 0%, rgba(8, 145, 178, 0.15) 100%);
  }

  .stat-purple {
    border-color: rgba(168, 85, 247, 0.4);
    color: #c084fc;
  }

  .stat-purple:hover {
    border-color: rgba(168, 85, 247, 0.8);
    box-shadow: 0 12px 48px rgba(168, 85, 247, 0.35), 0 0 0 1px rgba(168, 85, 247, 0.2) inset;
    background: linear-gradient(145deg, rgba(30, 41, 59, 0.9) 0%, rgba(147, 51, 234, 0.15) 100%);
  }

  .stat-green {
    border-color: rgba(34, 197, 94, 0.4);
    color: #4ade80;
  }

  .stat-green:hover {
    border-color: rgba(34, 197, 94, 0.8);
    box-shadow: 0 12px 48px rgba(34, 197, 94, 0.35), 0 0 0 1px rgba(34, 197, 94, 0.2) inset;
    background: linear-gradient(145deg, rgba(30, 41, 59, 0.9) 0%, rgba(22, 163, 74, 0.15) 100%);
  }

  /* SMALLER ICONS FOR PROBLEM SECTION */
  .stat-icon-small {
    width: 40px;
    height: 40px;
    margin: 0 auto 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--cyan-400);
  }

  .stat-number {
    font-size: 2.25rem;
    font-weight: 900;
    margin-bottom: 8px;
    background: linear-gradient(135deg, var(--blue-400) 0%, var(--cyan-400) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    line-height: 1;
  }

  .stat-label {
    color: var(--slate-300);
    font-size: 0.9375rem;
    font-weight: 500;
    line-height: 1.3;
  }

  /* Mobile optimization for compact stats */
  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 16px;
    }
    
    .stat-card {
      padding: 20px 12px;
      min-height: 160px;
    }
    
    .stat-number {
      font-size: 2rem;
    }
    
    .stat-label {
      font-size: 0.875rem;
    }
  }

  @media (max-width: 480px) {
    .stats-grid {
      grid-template-columns: 1fr;
      gap: 16px;
    }
    
    .stat-card {
      min-height: 140px;
    }
  }
</style>